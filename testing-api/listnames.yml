# API URL:s
urls:
- https://api.igdb.com/v4/companies
- https://api.igdb.com/v4/games
- https://api.igdb.com/v4/game_modes
- https://api.igdb.com/v4/game_types
- https://api.igdb.com/v4/genres
- https://api.igdb.com/v4/involved_companies
- https://api.igdb.com/v4/keywords
- https://api.igdb.com/v4/multiplayer_modes
- https://api.igdb.com/v4/platforms
- https://api.igdb.com/v4/player_perspectives
- https://api.igdb.com/v4/themes

# Queries for each URL
queries:
- fields name; where name != null; sort name asc; limit 999999;
- fields name, first_release_date, game_modes, game_type, genres, involved_companies, keywords, multiplayer_modes, platforms, player_perspectives, themes, hypes, total_rating, total_rating_count; where first_release_date > 1443477600 & hypes != null & total_rating != null & total_rating_count != null; sort first_release_date desc; limit 500;
- fields name; where name != null; sort name asc; limit 999999;
- fields type; where type != null; sort type asc; limit 999999;
- fields name; where name != null; sort name asc; limit 999999;
- fields company, developer, porting, publisher, supporting; where id != null; sort id asc; limit 999999;
- fields name; where name != null; sort name asc; limit 999999;
- fields campaigncoop, dropin, lancoop, offlinecoop, offlinecoopmax, offlinemax, onlinecoop, onlinecoopmax, onlinemax, splitscreen, splitscreenonline; where id != null; sort id asc; limit 999999;
- fields name; where name != null; sort name asc; limit 999999;
- fields name; where name != null; sort name asc; limit 999999;
- fields name; where name != null; sort name asc; limit 999999;